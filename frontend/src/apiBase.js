// Central API base URL for all frontend requests
// Uses deployed backend in production, relative path in development
// Force all API requests to use the deployed backend for local testing
export const API_BASE = "https://csgacha.onrender.com";
